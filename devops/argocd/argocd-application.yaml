apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: frankenphp
  annotations:
    # ArgoCD Image Updater: tự động phát hiện image mới trên DockerHub
    # Theo dõi digest của tag 'latest' - khi CI push image mới lên Docker Hub
    # Image Updater sẽ commit tag mới vào Git, ArgoCD tự sync deployment
    argocd-image-updater.argoproj.io/image-list: frankenphp=docker.io/devho/franken-php:latest
    argocd-image-updater.argoproj.io/frankenphp.update-strategy: digest
    # If your cluster does not run argocd/image-updater in the 'argocd' namespace,
    # set the pull-secret name without a namespace so ImageRepository/ImagePolicy
    # controllers running in the target namespace can reference it.
    argocd-image-updater.argoproj.io/frankenphp.pull-secret: regsecret
    argocd-image-updater.argoproj.io/write-back-method: git:secret:default/argocd-image-updater-git-creds
    argocd-image-updater.argoproj.io/git-branch: main
    argocd-image-updater.argoproj.io/frankenphp.force-update: "false"
spec:
  project: default
  source:
    repoURL: https://github.com/quynhhodev/symfony-frankenphp.git
    targetRevision: main
    path: devops
    directory:
      # Loại trừ thư mục argocd (bootstrap files) và fluxcd (đã thay thế)
      exclude: '{argocd/**,fluxcd/**}'
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  syncPolicy:
    automated:
      prune: true      # Xóa resource không còn trong Git
      selfHeal: true   # Tự phục hồi nếu cluster bị drift
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
